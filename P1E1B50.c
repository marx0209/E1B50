#include<stdlib.h>
#include<stdio.h>
#include<conio.h>
int main(void){
	char password[5],ch;
	
	//1.我的風格畫面
	int n=15,i,j,a;
	printf("        E1B50王楊馥華\n");
	//用for迴圈做一個金字塔
	for(i=0;i<n;i++)
	{
		for(j=0;j<n-i-1;j++)
		{
			printf(" ");
		}
	for(j=0;j<2*i+1;j++)
    	{
			printf("*");
		}
			printf("\n");// 換行
		}
	//列印附加的橫條圖形
	for(a=0;a<5;a++){
		printf("            *****\n");
		}
		getch();
		system("cls");
	
	//2.出現提示句並要求使用者輸入4個數字的密碼
		printf("請輸入4個數字的密碼");
	for(int i=1;i<5;i++){
        password[i] = getche();
    }
    //檢查密碼是否正確 (正確密碼為 "2025")
	if(password[1]=='2'&&password[2]=='0'&&password[3]=='2'&& password[4]=='5')
	{
		printf("\n歡迎！按任意鍵進入主選單~");
		getch();
		system("cls");
	}
	else{
	//密碼錯誤時發出警告音並顯示錯誤訊息
        	printf("\a\n輸入錯誤！\n");
        system("pause");
    	return 0;
    }
    
	// 3.清除螢幕，螢幕出現主選單：顯示字元判斷的選單，
	//使用者輸入一個字元後，程式判斷該字元是否為英文大寫 (A-Z)、英文小寫 (a-z)、數字 (0-9)，依照不同的情況輸出相應的訊息；若輸入其他符號，則輸出 "E1B50王楊馥華"。
    printf("---------------------------------------\n");
    printf("|    'A'...'Z' : Uppercase(英文大寫)   |\n");
    printf("|    'a'...'z' : Lowercase(英文小寫)   |\n");
    printf("|    '0'...'9' : Digit(數字)           |\n");
    printf("|    Otherwise : E1B50王楊馥華         |\n");
    printf("---------------------------------------\n");
    
	// 4.出現提示句並要求使用者輸入 1 個字元
    printf("請輸入一個字元: ");
    ch = getch();  //讀取一個字元

    if(ch >= 'A' && ch <= 'Z') {
        printf("Uppercase\n");
    }
    else if(ch >= 'a' && ch <= 'z') {
        printf("Lowercase\n");
    }
    else if(ch >= '0' && ch <= '9') {
        printf("Digit\n");
    }
    else {
	//若是其他符號字元，螢幕上出現你的班級座號及姓名。
        printf("E1B50王楊馥華 \n");
    }
	//待使用者按任何鍵後，結束程式回到作業系統。結束前提示使用者按任意鍵離開程式。 
    printf("請按任意鍵結束程式...\n");
    getch(); //按任意鍵結束程式
    return 0;
}
//討論與心得：

//1. 視覺化介面設計：
//利用巢狀 for 迴圈繪製樹，讓程式一開始就有一個可愛的小展示，
//這不僅讓使用者眼前一亮，也讓學習過程變得不那麼枯燥。

//2. 密碼驗證體驗：
//為了讓使用者看到輸入過程，我選用了 getche() 來顯示輸入的數字，
//雖然在實際密碼保護上可能不夠安全，但這裡主要是為了學習鍵盤輸入與字元儲存。

//3. 字元判斷邏輯：
//使用 getch() 讀取單一字元，簡單明瞭地使用條件判斷處理不同情況，這對我非常實用。

//4. 學習與成長：
//整個程式設計過程讓我從簡單的圖形輸出、密碼驗證到字元判斷，
//深入體會了 C 語言的基本語法、邏輯控制及使用者互動的重要性。
//希望這個程式不僅能幫助我理解程式邏輯，也能激發我對程式設計的興趣，
//每個小步驟都是進步的累積，勇敢嘗試，慢慢我也會寫出更複雜、更有趣的程式！
